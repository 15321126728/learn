# 类结构图

## 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                           Main.java                              │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  public static void main(String[] args)                   │  │
│  │  - 读取输入                                                │  │
│  │  - 调用Solver解析                                          │  │
│  │  - 输出结果                                                │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │ uses
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                         Solver.java                              │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  + Solver(String funcDef)                                 │  │
│  │  + Expr parse(String input)                               │  │
│  │  - Expr parseExpr()                                       │  │
│  │  - Expr parseTerm()                                       │  │
│  │  - Expr parseFactor()                                     │  │
│  │  - BigInteger parseBigInteger()                           │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
          │ uses                │ uses
          ↓                     ↓
┌──────────────────┐    ┌──────────────────┐
│  Function.java   │    │    Expr.java     │
│  ┌────────────┐  │    │  ┌────────────┐  │
│  │ + name     │  │    │  │ (abstract) │  │
│  │ + param    │  │    │  ├────────────┤  │
│  │ + body     │  │    │  │ + simplify()│  │
│  │ + call()   │  │    │  │ + isZero() │  │
│  └────────────┘  │    │  │ + substitute()│
└──────────────────┘    │  │ + equals() │  │
                        │  └────────────┘  │
                        └──────────────────┘
                                 △
                 ┌───────────────┼───────────────┬─────────────┐
                 │               │               │             │
                 │               │               │             │
    ┌────────────┴──────┐  ┌────┴──────┐  ┌────┴──────┐  ┌───┴─────────┐
    │  PolyExpr.java    │  │ExpExpr.java│  │BinaryExpr │  │SelectionExpr│
    │  ┌──────────────┐ │  │┌──────────┐│  │┌─────────┐│  │┌───────────┐│
    │  │+ terms       │ │  ││+ arg     ││  ││+ left   ││  ││+ cond1    ││
    │  │+ add()       │ │  ││+ power   ││  ││+ right  ││  ││+ cond2    ││
    │  │+ sub()       │ │  ││          ││  ││+ op     ││  ││+ trueExpr ││
    │  │+ multiply()  │ │  │└──────────┘│  │└─────────┘│  ││+ falseExpr││
    │  │+ pow()       │ │  └────────────┘  └───────────┘  │└───────────┘│
    │  │+ negate()    │ │                                  └─────────────┘
    │  └──────────────┘ │
    └───────────────────┘
```

## 类说明

### Expr.java (抽象基类)
所有表达式类型的父类，定义通用接口：
- `simplify()` - 化简表达式
- `isZero()` - 判断是否为零
- `substitute(var, value)` - 变量替换
- `equals(other)` - 判断两个表达式是否相等

### PolyExpr.java (多项式)
表示多项式表达式，如 `3*x^2 + 2*x + 1`
- 使用 `Map<Integer, BigInteger>` 存储项（指数→系数）
- 支持多项式的加减乘幂运算
- 193行代码

### ExpExpr.java (指数函数)
表示 exp 函数，如 `exp(x^2)` 或 `exp(x)^2`
- 存储参数和幂次
- 处理 exp(0) = 1 的特殊情况
- 76行代码

### BinaryExpr.java (二元运算)
表示二元运算，如 `a + b`、`a - b`、`a * b`
- 存储左右操作数和运算符
- 处理 exp 乘法合并：exp(a) * exp(b) → exp(a+b)
- 60行代码

### SelectionExpr.java (选择式)
表示条件选择，如 `[(x == 1) ? exp(x) : x]`
- 存储两个条件表达式和真假分支
- 通过相减判断条件是否相等
- 47行代码

### Function.java (自定义函数)
存储函数定义，如 `f(x) = x^2 + 1`
- 存储函数名、参数名、函数体
- 提供调用接口进行参数替换
- 17行代码

### Solver.java (解析器)
负责解析输入字符串并构建表达式树
- 递归下降解析器
- 支持表达式、项、因子的解析
- 215行代码

### Main.java (主程序)
程序入口，处理输入输出
- 读取函数定义数量
- 读取函数定义（如果有）
- 读取表达式
- 调用Solver解析并输出结果
- 47行代码

## 数据流

```
用户输入
  │
  ↓
Main.main()
  │
  ├─→ 读取函数定义 → Solver构造函数 → 解析函数体 → Function
  │
  └─→ 读取表达式 → Solver.parse()
                    │
                    ↓
               parseExpr() → parseTerm() → parseFactor()
                    │                           │
                    ↓                           ↓
              创建Expr子类对象 ← 创建PolyExpr/ExpExpr/BinaryExpr等
                    │
                    ↓
              result.simplify() → 递归化简所有子表达式
                    │
                    ↓
              result.toString() → 生成字符串输出
                    │
                    ↓
                输出结果
```

## 依赖关系

- Main → Solver, Expr
- Solver → Expr, PolyExpr, ExpExpr, BinaryExpr, SelectionExpr, Function
- Function → Expr
- BinaryExpr → Expr, PolyExpr, ExpExpr
- ExpExpr → Expr, PolyExpr
- PolyExpr → Expr, BinaryExpr
- SelectionExpr → Expr

## 继承关系

```
Expr (abstract)
 ├── PolyExpr
 ├── ExpExpr
 ├── BinaryExpr
 └── SelectionExpr
```

## 文件大小

| 文件 | 行数 | 大小 | 说明 |
|------|------|------|------|
| Expr.java | 11 | 0.4KB | 抽象基类 |
| PolyExpr.java | 193 | 6.5KB | 最复杂的类 |
| ExpExpr.java | 76 | 2.0KB | 中等复杂度 |
| BinaryExpr.java | 60 | 1.7KB | 较简单 |
| SelectionExpr.java | 47 | 1.3KB | 较简单 |
| Function.java | 17 | 0.4KB | 最简单 |
| Solver.java | 215 | 5.7KB | 第二复杂 |
| Main.java | 47 | 1.3KB | 简单 |
| **总计** | **666** | **19.3KB** | - |

对比原单文件版本：
- Main_single_file.java: 671行, 23KB

## 优势

1. **模块化** - 每个类职责单一
2. **可维护** - 修改一个类不影响其他
3. **可测试** - 可以独立测试每个类
4. **IDE友好** - 避免内部类的编译问题
5. **符合规范** - 遵循Java最佳实践

---

**说明**: 本文档展示了多文件版本的完整结构。所有类都是public的，可以跨文件访问。
