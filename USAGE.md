# 数学表达式解析器 (Mathematical Expression Parser)

本项目实现了一个完整的数学表达式解析器，符合"第四部分：基本概念"的规范要求。

## 编译和运行

### 编译
```bash
javac Main.java
```

### 运行
```bash
echo "x^2 + 2*x + 1" | java Main
```

或者交互式输入：
```bash
java Main
# 然后输入表达式，按回车
```

## 支持的功能

### 1. 带符号整数
支持前导 0 的十进制带符号整数，正号可省略。

**示例：**
- `+02` → 2
- `-16` → -16
- `20220928` → 20220928

### 2. 变量因子
支持以幂函数形式表达的变量 x。

**一般形式：**
- `x ^ +2` → x^2
- `x ^ 02` → x^2
- `x ^ 2` → x^2

**省略形式（指数为1时）：**
- `x` → x

### 3. 常数因子
支持带符号整数作为常数。

**示例：**
- `233` → 233
- `-1` → -1

### 4. 表达式因子
支持用括号包裹的表达式，可带指数。

**示例：**
- `(x^2 + 2*x + x)^2` → x^4+6*x^3+9*x^2
- `(x + 1)^3` → x^3+3*x^2+3*x+1

### 5. 项
支持用乘法运算符连接的因子，第一个因子前可带符号。

**示例：**
- `x * 02` → 2*x
- `+ x * 02` → 2*x
- `- +3 * x` → -3*x
- `x * x * x` → x^3

### 6. 表达式
支持用加减运算符连接的项。

**示例：**
- `-1 + x ^ 233 - x ^ 06 +x` → x^233-x^6+x-1
- `- -1 + x ^ 233` → x^233+1
- `+ -2 + x ^ 19911226` → x^19911226-2

### 7. 空白字符
自动忽略所有空格。

**示例：**
- `x  ^  2` → x^2
- `( x + 1 ) ^ 2` → x^2+2*x+1

## 测试

运行测试脚本：
```bash
chmod +x /tmp/run_tests.sh
/tmp/run_tests.sh
```

或使用提供的测试用例文件：
```bash
cat test_cases.txt
```

## 文档

- **IMPLEMENTATION.md** - 详细的实现说明
- **SUMMARY.md** - 项目完成总结
- **test_cases.txt** - 测试用例集合

## 输出格式

- 按指数降序排列项
- 系数为 1 时省略（如 `x` 而非 `1*x`）
- 系数为 -1 时显示为 `-x`
- 使用标准数学表示法

## 技术特点

- 递归下降解析器
- 支持任意大整数（BigInteger）
- 高效的多项式运算
- 完整的符号简化
- 清晰的代码结构

## 代码质量

✅ 通过代码审查  
✅ 通过 CodeQL 安全扫描（0 个漏洞）  
✅ 所有测试用例通过

## 许可证

MIT License
