# 多文件版本说明

## 概述

为了解决在IDEA等IDE中可能出现的编译问题，我们将原来的单文件Main.java拆分为多个独立的文件。

## 文件结构

### 核心类文件

1. **Expr.java** - 表达式抽象基类
   - 定义所有表达式类型的基本接口
   - 包含simplify(), isZero(), substitute()等抽象方法
   - 包含equals()方法用于表达式比较

2. **PolyExpr.java** - 多项式表达式类
   - 继承自Expr
   - 表示多项式（如 x^2 + 2*x + 1）
   - 包含加、减、乘、幂运算方法
   - 约6.5KB，193行代码

3. **ExpExpr.java** - 指数函数表达式类
   - 继承自Expr
   - 表示exp(...)函数
   - 支持幂次（如exp(x)^2）
   - 约2KB，76行代码

4. **BinaryExpr.java** - 二元运算表达式类
   - 继承自Expr
   - 表示加、减、乘运算
   - 处理exp函数的乘法合并优化
   - 约1.7KB，60行代码

5. **SelectionExpr.java** - 选择式表达式类
   - 继承自Expr
   - 表示条件选择 [(A==B)?C:D]
   - 约1.3KB，47行代码

6. **Function.java** - 自定义函数类
   - 存储函数定义
   - 处理函数调用和参数替换
   - 约400字节，17行代码

7. **Solver.java** - 解析器类
   - 负责解析输入字符串
   - 构建表达式树
   - 约5.7KB，215行代码

8. **Main.java** - 主程序入口
   - 包含main方法
   - 处理输入输出
   - 约1.3KB，47行代码

### 备份文件

- **Main_single_file.java** - 原始的单文件版本备份
  - 包含所有类的完整代码
  - 作为参考保留

## 主要变更

### 访问权限变更
- 所有类从 `private static class` 改为 `public class`
- 某些字段从 `private` 改为 `public`（如ExpExpr.arg, PolyExpr.terms）以支持跨文件访问

### 文件导入
每个文件根据需要导入必要的类：
- `import java.math.BigInteger;`
- `import java.util.*;`

## 编译和运行

### 编译所有文件
```bash
javac Expr.java PolyExpr.java ExpExpr.java BinaryExpr.java SelectionExpr.java Function.java Solver.java Main.java
```

或者简单地：
```bash
javac Main.java
```
（Java编译器会自动编译依赖的类）

### 运行程序
```bash
java Main
```

## 在IDEA中使用

1. 创建新的Java项目
2. 将所有.java文件复制到src目录
3. 刷新项目
4. 应该不会再有编译错误

## 测试验证

所有原有的测试用例都通过：

| 测试 | 输入 | 输出 | 状态 |
|------|------|------|------|
| 1 | `0`<br>`x+1` | `x+1` | ✅ |
| 2 | `0`<br>`exp(((x+1)^2))` | `exp((x^2+2*x+1))` | ✅ |
| 3 | `1`<br>`f(x) = x^2 + exp(0)`<br>`f((x+1))` | `x^2+2*x+2` | ✅ |

## 类依赖关系

```
Main
 └── Solver
      ├── Expr (abstract)
      │    ├── PolyExpr
      │    ├── ExpExpr
      │    ├── BinaryExpr
      │    └── SelectionExpr
      └── Function
```

## 优点

1. **更好的IDE支持** - 避免某些IDE对内部类的处理问题
2. **更清晰的结构** - 每个类独立一个文件，便于维护
3. **更好的可读性** - 类之间的关系更明显
4. **便于调试** - 可以单独测试每个类
5. **符合Java最佳实践** - 公共类独立文件

## 缺点

1. **文件数量增加** - 从1个文件变成8个文件
2. **部署稍复杂** - 需要确保所有文件都存在

## 建议

- 如果在命令行使用，两个版本都可以
- 如果在IDE中使用，建议使用多文件版本
- 提交作业时，可以根据要求选择使用单文件或多文件版本

## 注意事项

- 所有文件必须在同一个目录下（或同一个包中）
- 编译时确保所有依赖的类都被编译
- 如果修改了某个类，需要重新编译相关的依赖类

## 兼容性

- ✅ 完全兼容原有功能
- ✅ 所有测试用例通过
- ✅ 输出格式完全一致
- ✅ 中文注释保留

---

**创建日期**: 2026-03-12
**版本**: 1.0
