# 文件清单

## 核心代码文件（多文件版本）

### 必需的8个文件
以下文件必须全部复制到IDEA的src目录中：

| 文件名 | 行数 | 大小 | 说明 | 主要功能 |
|--------|------|------|------|----------|
| `Main.java` | 47 | 1.3KB | 主程序入口 | 读取输入，调用Solver，输出结果 |
| `Expr.java` | 11 | 0.4KB | 抽象基类 | 定义所有表达式的基本接口 |
| `PolyExpr.java` | 193 | 6.5KB | 多项式类 | 处理多项式运算（加减乘幂） |
| `ExpExpr.java` | 76 | 2.0KB | exp函数类 | 处理exp(x)表达式 |
| `BinaryExpr.java` | 60 | 1.7KB | 二元运算类 | 处理+、-、*运算 |
| `SelectionExpr.java` | 47 | 1.3KB | 选择式类 | 处理[(A==B)?C:D]表达式 |
| `Function.java` | 17 | 0.4KB | 函数类 | 存储和调用自定义函数 |
| `Solver.java` | 215 | 5.7KB | 解析器类 | 解析输入字符串，构建表达式树 |

**总计**: 666行代码，19.3KB

## 备份和参考文件

| 文件名 | 行数 | 大小 | 说明 |
|--------|------|------|------|
| `Main_single_file.java` | 671 | 23KB | 原始单文件版本备份 |

## 文档文件

| 文件名 | 大小 | 说明 |
|--------|------|------|
| `多文件版本说明.md` | 2.3KB | 详细说明为什么和如何拆分文件 |
| `类结构图.md` | 5.4KB | 展示类之间的关系和依赖 |
| `IDEA使用指南.md` | 3.6KB | 在IDEA中使用的详细步骤 |
| `编译问题排查指南.md` | 4.2KB | 编译错误的诊断和解决 |
| `快速使用指南.md` | 3.8KB | 程序的使用方法和示例 |
| `README_CN.md` | 2.2KB | 项目中文说明 |
| `问题解决报告.txt` | 20KB | 完整的问题分析报告 |

## 测试文件

| 文件名 | 大小 | 说明 |
|--------|------|------|
| `test.sh` | 2.8KB | Linux/Mac自动测试脚本 |
| `test.bat` | 2.8KB | Windows自动测试脚本 |

## 文件使用场景

### 场景1：在IDEA中使用（推荐）
**需要的文件**：
```
✅ Main.java
✅ Expr.java
✅ PolyExpr.java
✅ ExpExpr.java
✅ BinaryExpr.java
✅ SelectionExpr.java
✅ Function.java
✅ Solver.java
```

**步骤**：
1. 创建Java项目
2. 将上述8个文件复制到src目录
3. 运行Main.main()

**参考文档**：`IDEA使用指南.md`

### 场景2：命令行使用（单文件）
**需要的文件**：
```
✅ Main_single_file.java （重命名为Main.java）
```

**步骤**：
```bash
javac Main.java
java Main
```

### 场景3：命令行使用（多文件）
**需要的文件**：
```
✅ 所有8个.java文件
```

**步骤**：
```bash
javac Main.java  # 会自动编译依赖的类
java Main
```

## 文件依赖关系

```
Main.java
 └── 依赖 → Solver.java, Expr.java

Solver.java
 └── 依赖 → Expr.java, PolyExpr.java, ExpExpr.java, 
            BinaryExpr.java, SelectionExpr.java, Function.java

Function.java
 └── 依赖 → Expr.java

PolyExpr.java
 └── 依赖 → Expr.java, BinaryExpr.java
 └── 继承自 Expr

ExpExpr.java
 └── 依赖 → Expr.java, PolyExpr.java
 └── 继承自 Expr

BinaryExpr.java
 └── 依赖 → Expr.java, PolyExpr.java, ExpExpr.java
 └── 继承自 Expr

SelectionExpr.java
 └── 依赖 → Expr.java
 └── 继承自 Expr
```

## 复制指南

### 方法1：手动复制
1. 打开GitHub仓库
2. 依次打开每个文件
3. 复制内容
4. 在IDEA的src目录中创建对应的.java文件
5. 粘贴内容

### 方法2：克隆仓库
```bash
git clone https://github.com/15321126728/learn.git
cd learn
# 然后在IDEA中打开此目录
```

### 方法3：下载ZIP
1. 在GitHub上点击"Code" → "Download ZIP"
2. 解压
3. 在IDEA中打开项目目录

## 验证清单

### 编译验证
- [ ] 所有8个.java文件都在src目录中
- [ ] IDEA没有显示任何编译错误（红色下划线）
- [ ] 可以成功构建项目（Build → Build Project）

### 运行验证
- [ ] 可以运行Main.main()
- [ ] 输入 `0` 和 `x+1`，输出 `x+1`
- [ ] 输入 `0` 和 `exp(x)`，输出 `exp(x)`

### 功能验证
运行以下5个测试用例：

- [ ] 测试1: `0`, `x+1` → `x+1`
- [ ] 测试2: `0`, `exp(((x+1)^2))` → `exp((x^2+2*x+1))`
- [ ] 测试3: `0`, `exp(x)*exp((2*x))` → `exp((x+2*x))`
- [ ] 测试4: `0`, `[((x-1)^2)==(x^2-2*x+1)?exp(x):x]` → `exp(x)`
- [ ] 测试5: `1`, `f(x)=x^2+exp(0)`, `f((x+1))` → `x^2+2*x+2`

## 文件编码

所有文件都使用UTF-8编码，包含中文注释。

确保IDEA的编码设置为UTF-8：
- File → Settings → Editor → File Encodings
- Global Encoding: UTF-8
- Project Encoding: UTF-8

## 注意事项

### 必须一起使用的文件
8个核心代码文件必须全部存在，缺少任何一个都会导致编译错误：

```
Main.java ← 需要 Solver.java, Expr.java
Solver.java ← 需要 所有其他6个类
PolyExpr.java ← 需要 Expr.java, BinaryExpr.java
ExpExpr.java ← 需要 Expr.java, PolyExpr.java
BinaryExpr.java ← 需要 Expr.java, PolyExpr.java, ExpExpr.java
SelectionExpr.java ← 需要 Expr.java
Function.java ← 需要 Expr.java
Expr.java ← 被所有其他类依赖
```

### 文件名必须匹配类名
- Main.java 必须包含 `public class Main`
- Expr.java 必须包含 `public abstract class Expr`
- 其他文件同理

### 不要混用两个版本
- 不要同时使用 Main.java 和 Main_single_file.java
- 选择其中一个版本即可

## 推荐阅读顺序

1. **IDEA使用指南.md** - 如何在IDEA中使用
2. **多文件版本说明.md** - 了解为什么拆分文件
3. **类结构图.md** - 理解代码结构
4. **快速使用指南.md** - 学习如何使用程序
5. **编译问题排查指南.md** - 遇到问题时查看

## 获取帮助

如果遇到问题：
1. 查看 `IDEA使用指南.md`
2. 查看 `编译问题排查指南.md`
3. 确保所有8个文件都已复制
4. 检查IDEA的编码设置
5. 确认Java版本 >= 8

---

**文件清单版本**: 1.0  
**最后更新**: 2026-03-12
