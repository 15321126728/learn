# 快速参考指南 (Quick Reference Guide)

## 文档导航

本项目包含以下文档，按阅读顺序推荐：

### 1. 入门文档
- **[README.md](README.md)** - 项目简介和基本使用
- **[USAGE.md](USAGE.md)** - 详细使用指南和示例

### 2. 架构文档
- **[ARCHITECTURE.md](ARCHITECTURE.md)** ⭐ **推荐首先阅读**
  - 系统概览
  - 架构设计（三层架构）
  - 核心组件详解
  - 设计模式分析
  - 数据流说明
  - 关键算法讲解
  - 性能分析

- **[ARCHITECTURE_DIAGRAMS.md](ARCHITECTURE_DIAGRAMS.md)** - 架构可视化
  - 系统分层图
  - 类关系图
  - 解析流程图
  - 数据结构图
  - 算法流程图

### 3. 实现文档
- **[IMPLEMENTATION.md](IMPLEMENTATION.md)** - 实现细节
  - 基本概念的实现方式
  - 规范要求的代码映射
  - 测试用例说明

### 4. 总结文档
- **[SUMMARY.md](SUMMARY.md)** - 项目完成总结
- **[test_cases.txt](test_cases.txt)** - 测试用例集合

---

## 核心概念速查

### 三大核心类

```
Main → Solver → Poly
 ↓       ↓       ↓
入口   解析器   多项式
```

#### Main 类
- **作用**: 程序入口
- **主要方法**: `main(String[])`
- **职责**: I/O处理

#### Solver 类
- **作用**: 表达式解析器
- **主要方法**: 
  - `solve()` - 执行解析
  - `parseExpr()` - 解析表达式
  - `parseTerm()` - 解析项
  - `parseFactor()` - 解析因子
- **职责**: 字符串 → 多项式对象

#### Poly 类
- **作用**: 多项式表示
- **主要方法**:
  - `add(Poly)` - 加法
  - `multiply(Poly)` - 乘法
  - `pow(int)` - 乘方
  - `toString()` - 格式化
- **职责**: 代数运算

---

## 解析流程速查

```
输入字符串
    ↓
【预处理】去空格、简化符号
    ↓
【parseExpr】解析表达式（处理+/-）
    ↓
【parseTerm】解析项（处理*和符号）
    ↓
【parseFactor】解析因子（变量/常数/表达式）
    ↓
【Poly运算】加减乘幂
    ↓
【toString】格式化输出
    ↓
输出结果
```

---

## 数据结构速查

### Poly 内部表示
```java
Map<Integer, BigInteger> terms
// Key: 指数, Value: 系数

示例:
3x^2 - 2x + 1
↓
{2: 3, 1: -2, 0: 1}
```

### Solver 状态
```java
String expr  // 预处理后的表达式
int pos      // 当前解析位置
```

---

## 常用操作速查

### 编译运行
```bash
javac Main.java
echo "x^2+2*x+1" | java Main
```

### 支持的表达式
```
变量: x, x^2, x^100
常数: 123, -45, +02
表达式: (x+1)^2, (x^2+x)^3
运算: +, -, *
```

### 输出格式
```
降幂排列: x^3+x^2+x+1
系数为1: x (不是 1*x)
系数为-1: -x (不是 -1*x)
负系数: x^2-2*x+1
```

---

## 关键算法速查

### 1. 递归下降解析
- **特点**: 自顶向下，无回溯
- **复杂度**: O(n)
- **优势**: 代码清晰，易扩展

### 2. 多项式乘法
- **算法**: 双重循环，指数相加，系数相乘
- **复杂度**: O(n×m)
- **优化**: 使用HashMap自动合并同类项

### 3. 符号简化
- **规则**: ++→+, --→+, +-→-, -+→-
- **方法**: 循环替换直到不变
- **目的**: 简化解析逻辑

---

## 设计模式速查

| 模式 | 应用位置 | 说明 |
|-----|---------|-----|
| 递归下降 | Solver解析方法 | 文法与代码对应 |
| 不可变对象 | Poly类 | 运算返回新对象 |
| 策略模式 | parseFactor | 不同因子不同策略 |
| 构建器模式 | Poly内部 | 逐步构建多项式 |

---

## 性能特点速查

| 操作 | 时间复杂度 | 空间复杂度 |
|-----|-----------|-----------|
| 解析 | O(n) | O(n) |
| 加法 | O(n+m) | O(n+m) |
| 乘法 | O(n×m) | O(n×m) |
| 乘方 | O(k×n²) | O(kn) |
| 格式化 | O(n log n) | O(n) |

---

## 扩展建议

### 容易扩展
✅ 添加新运算符（/, %, ^）
✅ 支持新函数（sin, cos, log）
✅ 多变量支持（x, y, z）

### 当前限制
⚠️ 只支持单变量 x
⚠️ 只支持整数系数
⚠️ 只支持多项式运算

---

## 调试技巧

### 1. 查看预处理结果
在Solver构造函数末尾添加:
```java
System.out.println("预处理: " + expr);
```

### 2. 跟踪解析过程
在parse方法开头添加:
```java
System.out.println("解析位置: " + pos + ", 字符: " + peek());
```

### 3. 查看Poly内容
```java
System.out.println(poly.terms);
```

---

## 常见问题

### Q1: 为什么需要符号简化？
**A**: 简化 `- -1` 为 `+1`，使解析更简单。

### Q2: 为什么使用HashMap存储？
**A**: O(1)查找，自动合并同类项，支持稀疏多项式。

### Q3: 为什么Poly是不可变的？
**A**: 避免副作用，线程安全，易于推理。

### Q4: 递归下降和其他解析方法的区别？
**A**: 递归下降代码简单直观，但不支持左递归文法。

---

## 学习路径建议

### 初学者
1. 阅读 **USAGE.md** 了解如何使用
2. 运行几个示例理解功能
3. 阅读 **ARCHITECTURE.md** 的"系统概览"和"核心组件"
4. 查看 **ARCHITECTURE_DIAGRAMS.md** 的"解析流程图"
5. 逐个阅读 Main.java 的三个类

### 进阶学习
1. 完整阅读 **ARCHITECTURE.md**
2. 理解递归下降解析原理
3. 研究多项式运算算法
4. 尝试扩展新功能
5. 优化性能瓶颈

### 架构研究
1. 分析三层架构的优势
2. 理解设计模式的应用
3. 研究数据流转过程
4. 评估性能特点
5. 思考改进方案

---

## 相关资源

### 理论基础
- 编译原理 - 递归下降解析
- 数据结构 - 哈希表应用
- 计算机代数系统

### 实践项目
- 扩展为科学计算器
- 添加微积分功能
- 实现多变量支持

---

## 联系与贡献

如有问题或建议，欢迎：
- 提交 Issue
- 发起 Pull Request
- 参与讨论

---

**最后更新**: 2026-03-10
