# 快速使用指南

## 项目说明
这是一个表达式解析和化简程序，支持多项式、指数函数、自定义函数等。

## 验证代码是否正常

### 方法1：使用自动测试脚本（推荐）

#### Windows用户：
双击运行 `test.bat` 文件，或在命令行中运行：
```cmd
test.bat
```

#### Linux/Mac用户：
在终端中运行：
```bash
./test.sh
```

如果所有测试都显示 ✅，说明代码完全正常！

### 方法2：手动编译和测试

#### 第1步：编译
```bash
javac -encoding UTF-8 Main.java
```

如果没有错误信息，说明编译成功。

#### 第2步：测试运行
```bash
# 测试简单表达式
echo -e "0\nx+1" | java Main
# 应该输出：x+1

# 测试exp函数
echo -e "0\nexp(x)" | java Main
# 应该输出：exp(x)

# 测试函数定义
echo -e "1\nf(x) = x^2\nf(x+1)" | java Main
# 应该输出：x^2+2*x+1
```

## 遇到编译错误？

如果您遇到编译错误，请查看 **[编译问题排查指南.md](编译问题排查指南.md)**

该指南包含：
- 常见问题的详细分析
- 5种解决方案
- IDE配置建议
- 完整的检查清单

## 程序使用方法

### 输入格式
```
第1行：函数定义的个数（0或1）
第2行（如果第1行是1）：函数定义，格式为 f(x) = 表达式
最后1行：要化简的表达式
```

### 示例1：简单多项式
```
输入：
0
x^2 + 2*x + 1

输出：
x^2+2*x+1
```

### 示例2：exp函数展开
```
输入：
0
exp(((x+1)^2))

输出：
exp((x^2+2*x+1))
```

### 示例3：exp乘法优化
```
输入：
0
exp(x)*exp((2*x))

输出：
exp((x+2*x))
```

### 示例4：自定义函数
```
输入：
1
f(x) = x^2 + exp(0)
f((x+1))

输出：
x^2+2*x+2
```

### 示例5：选择式因子
```
输入：
0
[( ((x-1)^2) == (x^2-2*x+1) ) ? exp(x) : x]

输出：
exp(x)
```

## 支持的功能

### 1. 多项式运算
- 加法：`x + 1`
- 减法：`x - 1`
- 乘法：`x * 2`
- 幂运算：`x^2`

### 2. 指数函数
- 基本：`exp(x)`
- 带幂次：`exp(x)^2`
- 乘法合并：`exp(a) * exp(b)` → `exp((a+b))`

### 3. 自定义函数
- 定义：`f(x) = x^2 + 1`
- 调用：`f(x+1)` → `(x+1)^2+1` → `x^2+2*x+2`

### 4. 选择式因子
- 格式：`[(A == B) ? C : D]`
- 如果 A 等于 B，返回 C，否则返回 D

## 项目结构

```
learn/
├── Main.java                   # 主程序（所有代码都在这个文件中）
├── test.sh                     # Linux/Mac测试脚本
├── test.bat                    # Windows测试脚本
├── 编译问题排查指南.md          # 详细的问题排查指南
├── COMMENTS_TRANSLATION.md     # 注释翻译说明
├── SUMMARY_CN.md               # 中文总结文档
└── 其他文档...
```

## 常见问题 FAQ

### Q1: 编译时提示"非法的表达式开始"
**A:** 请查看 [编译问题排查指南.md](编译问题排查指南.md)，按照步骤排查。最常见的原因是：
- 复制粘贴时的字符问题
- IDE编码设置不是UTF-8
- Java版本不兼容

### Q2: 如何确认代码是否正常？
**A:** 运行自动测试脚本（test.sh 或 test.bat）。如果所有测试通过，说明代码正常。

### Q3: 支持哪些Java版本？
**A:** 建议使用Java 8或更高版本。

### Q4: 为什么我的输出和示例不一样？
**A:** 请检查：
1. 输入格式是否正确
2. 是否使用了正确的运算符（`*` 而不是 `×`）
3. 括号是否匹配

### Q5: 如何重新获取完整代码？
**A:** 从GitHub克隆：
```bash
git clone https://github.com/15321126728/learn.git
```

## 技术支持

如果遇到问题：
1. 首先查看 [编译问题排查指南.md](编译问题排查指南.md)
2. 运行测试脚本验证代码
3. 检查输入格式是否正确
4. 确认Java环境配置正确

---

**最后更新**: 2026-03-12
